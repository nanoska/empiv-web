# # tickets/urls.py
# from django.urls import path
# from .views import *

# urlpatterns = [
#     path('', TicketListView.as_view(), name='ticket_list'),
#     path('new/', TicketCreateView.as_view(), name='ticket_create'),
#     path('<int:pk>/', TicketDetailView.as_view(), name='ticket_detail'),
#     path('<int:pk>/chat/', TicketChatCreateView.as_view(), name='ticket_chat'),
#     path('<int:pk>/resolve/', MarkTicketResolvedView.as_view(), name='mark_ticket_resolved'),
#     path('<int:pk>/unresolve/', MarkTicketUnresolvedView.as_view(), name='mark_ticket_unresolved'),
# ]
